package net.raumzeitfalle.gradle.gocd.versioning;

public enum GitlabCICD {
    CI;

    /*
     * CI_SERVER_URL,
     * CI_ENVIRONMENT_NAME,
     * CI_PROJECT_NAME,
     * CI_JOB_ID,
     * CI_JOB_NAME,
     * CI_JOB_STAGE,
     * GITLAB_USER_NAME
     */
}
